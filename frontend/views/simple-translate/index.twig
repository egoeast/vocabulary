{{ use('yii\helpers\Html') }}
{{ use('yii\widgets\ActiveForm') }}

<div class="">

    {{ lang('frontend', 'Hello') }}

    {% set form = active_form_begin({
    'id' : 'translation-form',


    })%}
    {{ form.field(translation, 'id_voc').label(lang('frontend', 'Vocabulary')) | raw }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-6">
                {{ form.field(translation, 'text') | raw }}
            </div>
            <div class="col-md-6">
                {{ form.field(translation, 'translation') | raw }}
            </div>
            </div>
        </div>
    </div>
    <div class="form-group">

        <!--<?= Html::submitButton(Yii::t('app', 'Submit'), ['class' => 'btn btn-primary']) ?>-->
        {{ html.a(lang('app', 'Translate'), '#', {'class':'btn btn-default trans-btn'}) | raw }}

        <input type="submit" value="Сохранить" class="btn btn-primary" />
    </div>
    {{ active_form_end() }}
</div><!-- weather-station-index -->

